apiVersion: v1
kind: Service
metadata:
  name: metadata-service-kind
  labels:
    app: metadata
spec:
  type: NodePort
  selector:
    app: metadata
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
      name: http